Rename the .env.example file to .env and change config if needed. (Not necessary)
Run the following commands using Powershell:
-npm i
-composer i
-php artisan migrate
-php artisan key:generate
-npm run dev
-php artisan serve
